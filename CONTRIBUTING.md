# Contributing

Every standards change must:

1. identify the affected standard and compatibility boundary;
2. distinguish normative requirements from examples;
3. include schema and adversarial-test changes when machine contracts change;
4. record downstream adopters and unresolved deviations;
5. arrive through a focused pull request with exact validation evidence.

Candidate text is not binding until an adoption record pins an accepted
version and commit.
